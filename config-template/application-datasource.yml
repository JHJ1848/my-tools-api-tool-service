# ============================================
# 此文件已弃用，请使用分模块配置文件
# ============================================
# 新的配置方式:
#   - application-dameng.yml  (达梦数据库配置)
#   - application-md.yml      (Markdown预览配置)
#   - application-mcp.yml     (MCP服务配置)
#
# 将上述文件复制到运行目录的 config/ 下并填入实际配置
