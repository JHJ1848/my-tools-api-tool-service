# 项目变更日志 (Changelog)

本文档记录了 `tool-service` 项目 Markdown 预览功能的更新历史。

## [v1.0.7] - 2026-03-04
### 新增 (Added)
- **文件下载功能 (File Download)**：
    - 提供了 `/md-download` 接口，支持直接下载当前预览的 Markdown 文件。
    - 顶部工具栏新增“下载”按钮 (📥)。
- **原文本查看 (Raw Content)**：
    - 提供了 `/md-content` 接口，以纯文本形式返回 Markdown 内容。
    - 顶部工具栏新增“预览模式”与“原文本”切换按钮，方便查看源码。
- **高级标签页控制**：
    - 标签页支持右键菜单：
        - 关闭当前标签页 (Close Current)。
        - 关闭所有标签页 (Close All)。
    - 支持快捷键 `Ctrl+W` 关闭当前标签页。
### 优化 (Changed)
- **滚动体验优化**：
    - 移除了点击文件时的自动居中滚动，避免页面闪烁。
    - 实现了侧边栏滚动位置的持久化，刷新页面后保持滚动条位置不变。

## [v1.0.6] - 2026-03-02
### 新增 (Added)
- **文件与文件夹图标**：
    - 为目录树中的文件夹添加了文件夹图标（金色）。
    - 为 Markdown 文件添加了文档图标（蓝色）。
    - 优化了目录树的视觉层级和对齐方式。

## [v1.0.5] - 2026-03-02
### 新增 (Added)
- **多标签页支持 (Multi-tabs)**：
    - 实现了类似 IDE 的顶部标签页功能。
    - 支持同时打开多个文档，并在标签之间切换。
    - 标签页状态自动保存（基于 LocalStorage），刷新页面不丢失。
    - 支持关闭标签页，关闭后自动跳转到上一个标签。

### 优化 (Changed)
- **左侧路径显示**：
    - 缩小了左侧栏当前文件路径的字体大小，以适应长路径显示。
    - 优化了路径截断方式，鼠标悬停时可查看完整路径。

## [v1.0.4] - 2026-03-02
### 新增 (Added)
- **定位功能 (Locate)**：
    - 在左侧栏增加了“定位到当前文件”按钮 (🎯)。
    - 点击按钮可自动展开目录树并滚动到当前预览的文件位置。
    - 定位操作会自动清除当前的搜索过滤状态。
- **当前路径显示**：
    - 在左侧栏顶部显示当前预览文件的相对路径。

## [v1.0.3] - 2026-03-02
### 新增 (Added)
- **侧边栏搜索功能**：
    - 增加了文件搜索框。
    - 支持实时过滤文件列表（按文件名）。
    - 搜索命中时自动展开父级目录，并高亮显示匹配项。

## [v1.0.2] - 2026-03-02
### 修复 (Fixed)
- **目录状态保持**：
    - 修复了点击文件后目录树自动收起的问题。
    - 实现了目录展开/折叠状态的客户端持久化（LocalStorage）。
    - 页面刷新或跳转后，目录树保持之前的展开状态。

## [v1.0.1] - 2026-03-02
### 修复 (Fixed)
- **深层目录显示问题**：
    - 修复了深层目录下的 Markdown 文件无法显示的问题。
    - 将目录扫描的递归深度限制从 5 层增加到了 10 层。

## [v1.0.0] - 2026-03-02
### 初始化 (Init)
- **基础预览功能**：
    - 实现了基于 Spring Boot 的 Markdown 文件预览服务。
    - 支持左侧目录树导航和右侧 Markdown 内容渲染。
    - 支持图片资源的相对路径解析。
