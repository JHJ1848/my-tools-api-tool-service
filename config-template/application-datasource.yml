# 达梦数据库外部配置文件模板
# 将此文件复制到与jar包同级的config目录下

spring:
  datasource:
    dynamic:
      primary: master
      datasource:
        master:
          username: SYSDBA
          password: mll123!@#
          driver-class-name: dm.jdbc.driver.DmDriver
          url: jdbc:dm://127.0.0.1:5236/SOIL_SURVEY_DATA_APPLY?useUnicode=true&characterEncoding=utf-8
        sso:
          username: SYSDBA
          password: mll123!@#
          driver-class-name: dm.jdbc.driver.DmDriver
          url: jdbc:dm://127.0.0.1:5236/SOIL_SURVEY_DATA_APPLY?useUnicode=true&characterEncoding=utf-8

server:
  port: 8080
